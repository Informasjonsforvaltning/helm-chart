{{- range .Values.kinds }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fdk-{{ .kind }}-diff-store
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
{{- end }}
