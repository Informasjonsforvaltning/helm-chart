# Default values for fdk-metadata-quality-service.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

##fdk-metadata-quality-service
FDK_METADATA_QUALITY_SERVICE_MEMORY: "500Mi"