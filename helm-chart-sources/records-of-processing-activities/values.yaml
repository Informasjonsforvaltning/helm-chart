# Default values for a-backend-service.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

##a-template-service (api)
RECORD_OF_PROCESSING_ACTIVITIES_MEMORY: "1000Mi"